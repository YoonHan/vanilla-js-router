import { expect, describe, it } from '@jest/globals';

describe('Hash Router 클래스', () => {
    describe('HashRouter 인스턴스를 생성할 수 있다.', () => {
        it('options 객체없이 생성자를 호출하는 경우, options.root와 options.view는 기본값으로 세팅된다.', {
            const hashrouter = shallow
        })
    })
});